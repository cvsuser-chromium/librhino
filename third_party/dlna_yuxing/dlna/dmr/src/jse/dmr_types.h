#ifndef __DMR__TYPES__
#define __DMR__TYPES__


typedef int TransportPlaySpeed;
typedef unsigned int uint;
typedef int AVTResult;

typedef enum _SeekMode
{
	SEEK_MODE_PLAY_TIME = 0,
	SEEK_MODE_PLAY_OFFSET,
	SEEK_MODE_SUBTILE_PID,
	SEEK_MODE_AUDIO_PID,
	SEEK_MODE_MUTE,
	SEEK_MODE_VOLUME,
	SEEK_MODE_AUDIO_CHANNEL,
}SeekMode;

typedef enum _PlayMode
{
	PLAY_MODE_NORMAL = 0,
	PLAY_MODE_RANDOM,
	PLAY_MODE_REPEAT_ONE,
	PLAY_MODE_REPEAT_ALL,
}PlayMode;

typedef enum _TransportState
{
	TRANSPORT_STATE_NO_MEDIA_PRESENT = 0,
	TRANSPORT_STATE_STOPPED,
	TRANSPORT_STATE_PLAYING,
	TRANSPORT_STATE_TRANSITIONING,
	TRANSPORT_STATE_PAUSED_PLAYBACK,
	TRANSPORT_STATE_FFW_PLAYBACK,
	TRANSPORT_STATE_FRW_PLAYBACK,
}TransportState;

typedef enum _MediaCategory
{
	MEDIA_CATEGORY_NO_MEDIA = 0,
	MEDIA_CATEGORY_TRACK_AWARE,
	MEDIA_CATEGORY_TRACK_UNAWARE,
}MediaCategory;

typedef enum _TransportStatus
{
	TRANSPORT_STATUS_OK = 0,
	TRANSPORT_STATUS_ERROR,
}TransportStatus;

typedef enum _DMR_MSG
{
	DMR_MSG_NONE = 0,
	DMR_MSG_URI_CHANGED = 1,
	DMR_MSG_TRACK_CHANGED = 2,
	DMR_MSG_PLAYMODE_CHANGED = 4,
	DMR_MSG_TRANSPORT_STATE_CHANGED = 8,
	DMR_MSG_TRANSPORT_ACTION_CHANGED = 16,
	DMR_MSG_TRANSPORT_STATUS_CHANGED = 32,
}DMR_MSG;


enum
{
	TRANSITION_NOT_AVAILABLE = -100,
	NO_CONTENTS,
	READ_ERROR,
	FORMAT_NOT_SUPPORTED_FOR_PLAYBACK,
	TRANSPORT_IS_LOCKED,
	SEEK_MODE_NOT_SUPPORTED,
	ILLEGAL_SEEK_TARGET,
	PLAY_MODE_NOT_SUPPORTED,
	ILLEGAL_MIME_TYPE,
	CONTENT_BUSY,
	RESOURCE_NOT_FOUND,
	PLAY_SPEED_NOT_SUPPORTED,
	EXPIRED_CONTENT,
	NON_ALLOWED_USE,
	CAN_NOT_DETERMINE_ALLOWED_USES,
	EXHAUSTED_ALLOWED_USE,
	DEVICE_AUTHENTICATION_FAILURE,
	DEVICE_REVOCATION,
	AVT_INTERNAL_ERROR,
	AVT_FUNCTION_NOT_SUPPORT,
	AVTRESULT_ERROR_UNKNOW = -1,
	
	AVTRESULT_OK = 0,
};



typedef enum _RCSResult
{
	INVALID_NAME = -100,
	INVALID_INSTANCEID,
	INVALID_CHANNEL,
	INVALID_MEDIARENDERER_UDN, 
	RCSRESULT_ERROR_UNKNOW = -1,
	
	RCSRESULT_OK = 0,
}RCSResult;

typedef enum _DirectionType
{
	DIRECTION_OUTPUT = 0,
	DIRECTION_INPUT,
}DirectionType;

typedef enum _ConnectionStatus
{
	CONNECTION_STATUS_OK = 0,
	CONTENT_FORMAT_MISMATCH,
	INSUFFICIENT_BANDWIDTH,
	UNRELIABLE_CHANNEL,
	CONNECTION_STATUS_UNKNOW,
}ConnectionStatus;

typedef enum _CMResult
{
	INCOMPATIBLE_PROTOCOL_INFO = -100,
	INCOMPATIBLE_DIRECTIONS,
	INSUFFICIENT_NETWORK_RESOURCES,
	LOCAL_RESTRICTIONS,
	ACCESS_DENIED,
	INVALID_CONNECTION_REFERENCE,
	NOT_IN_NETWORK,
	CONNECTION_TABLE_OVERFLOW,
	INTERNAL_PROCESSING_RESOURCES_EXCEEDED,
	INTERNAL_MEMORY_RESOURCES_EXCEEDED,
	INTERNAL_STORAGE_SYSTEM_CAPABILITIES_EXCEEDED,
	CMRESULT_ERROR_UNKNOW = -1,
	
	CMRESULT_OK = 0
}CMResult;


#endif

